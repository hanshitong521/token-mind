# 国内镜像（最快源）：pnpm/npm 安装依赖走 npmmirror，避免官方源慢/超时。
# ContextMind 本体零运行时依赖（ADR-0001），此文件仅为该包内任何 node 依赖安装加速。
registry=https://registry.npmmirror.com
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=60000
prefer-online=true