# Token-Mind × Agent Stack Peak × TestMind — 问题总表与满分（9/10）规格

> **文档性质**：缺陷 SSOT + 满分验收规格（非进度喜报）。  
> **更新**：2026-09-10  
> **依据**：[`TestMind_真实源码全量审计与跨AI极致测试整改报告_20260910.md`](../TestMind_真实源码全量审计与跨AI极致测试整改报告_20260910.md)、[`shared/pipeline-contract.yaml`](../shared/pipeline-contract.yaml)、[`shared/docs/AGENT_STACK_PEAK.md`](../shared/docs/AGENT_STACK_PEAK.md)、工作区 `test-Mind/` 附录 C 实测、本仓 README/报告。  
> **Canonical 源码**：TestMind = [`test-Mind/`](../test-Mind/)；CTX = 本仓 `Token-Mind/`。

---

## 0. 满分（9/10）定义 — 必须全部满足才可宣称「生产级 VERIFY + CTX」

| # | 满分条件 | 可测量验收 |
|---|----------|------------|
| F1 | **真实 consumer 闭环**：至少 **2 个不同栈**业务仓（含 1 个 Java/Spring）从 TaskBundle v2 → AI-Code commit → TestMind intake → 四件套 → `export_handoff` → Brain 只读 verified 回写，**无聊天记录 SSOT** | 每仓 `.testmind/tasks/<task_id>/` 可复现；证据 manifest hash 可校验 |
| F2 | **门禁对外承诺**：Linux + Windows CI 每 PR **blocking 绿**；`pipeline-contract.yaml` 所列门（RM/G/B/TM/INT）均有脚本、信封字段完整（INV-005） | `run_peak_verify` + 栈级 rollup；TM3 在 Linux **114/114 真跑**，非 Windows 代位 |
| F3 | **PASS 新鲜度（INV-006）**：任何 PASS 绑定 `git_head` + `pipeline_contract_hash` + `evidence_manifest_hash`；变更后 **STALE** 在 **agent-stack-peak 聚合 UI/报告**可见 | 改 contract 或 commit 后旧 PASS 不可当放行依据 |
| F4 | **零假绿**：required runner/case 不可用 → **BLOCKED/NOT_TESTED**；`TOOL_ERROR` 不得当 SUT FAIL；production **destructive/fault/load** 一律拒 | 合成 secret grep=0；production prepare=BLOCKED |
| F5 | **Agent 面可机读**：对外 **10–12** 个强类型 MCP 工具；100% 真实 JSON Schema + contract tests；禁止 `{args:{}}` 包装 | schema 驱动单测 100% 工具覆盖 |
| F6 | **Java/中间件深度**：Controller/DTO/Validation/Service/Mapper/XML + Redis + 下游 fault 进入 **Risk Router required 矩阵** | shejiuPro（或等价）compile/unit/API/DB/Redis/并发/perf 全链绿 |
| F7 | **性能与覆盖**：k6 分阶段 smoke + JaCoCo（或等价）阈值进 Gate；非「32 线程数 2xx」冒充压测 | TM 扩展门或 INT 门 blocking |
| F8 | **Token-Mind G0–G8 全绿** + TaskBundle `spec.verify` 与 handoff 强制字段 | ContextMind SDLC + stack-router 实测 |
| F9 | **仓职责纯净**：TestMind 仓无 RequirementMind/恢复事故 JSON；shared 依赖 **vendor/submodule**，独立克隆可跑 gate | W-P0-13 完成；clone 仅 test-Mind 可 TM0–TM9 |
| F10 | **30 天运营指标**：≥30 真实 task、漏测率/误报率/ flaky 有仪表盘；Prompt 压缩 **不得**删 TestMind Skill 四条硬规则 | Brain + 基准集 §14 P2-7 |

**当前综合评分（诚实）**

| 视角 | 分数 | 裁决 |
|------|------|------|
| 理念 / 红包靶场 / P0 内核（工作区） | 7–7.5 | 局部强 |
| PILOT（附录 C 已填） | **6–6.5** | 可试点，非唯一放行 |
| 对标上表 F1–F10 满分 | **≈4–5** | **远未到 9 分** |
| GitHub `test-mind@9ab2403` 审计基线 | **≈3.5 HOLD** | 镜像未同步则仍属历史 |

---

## 1. Token-Mind（CTX 层）— 满分规格 vs 缺陷

### 1.1 产品状态（自述）

- README：**S2+S3 切片上线，产品未完成**；**spec G0–G8 未全部执行**。
- 角色：`pipeline-contract` → `token_mind.role: ctx_owner`；Gate **G0–G8 blocking**。

### 1.2 满分规格（G0–G8 + CTX 职责）

| 门/能力 | 满分要求 | 当前问题 |
|---------|----------|----------|
| **G0–G8** | 与 [`shared/docs/AGENT_STACK_PEAK.md`](../shared/docs/AGENT_STACK_PEAK.md) 一致：每门有脚本、证据、`gate-envelope`（component_version, commit, contract_hash, input_hash, measured_at） | **未全执行**；栈 rollup 不能宣称 Peak Core 完整 |
| **TaskBundle v2** | `schema_version: 2`；`task_id` + `requirement.session_id`（INV-009）；`spec.verify` 嵌入 change_manifest / environment_ref / decision_context_ref | 已有 [`task-bundle.verify.schema.json`](contextmind/sdlc/task-bundle.verify.schema.json)；**与 SDLC `task.active` 路由到 TestMind MCP 的端到端产品流未满分** |
| **verify 切片** | change_manifest 含 `base_sha/head_sha`、endpoints、risks、verification_hints；禁止 TestMind 仓内第二套 SSOT | 开发 AI 产出 manifest 的 **强制钩子**未在 ai-code 仓统一；shejiuPro **无 INT 全链实测** |
| **handoff** | `handoff_summary.must_read`：**TEST_REPORT 摘要**、FAIL 时 **FAILURE_BUNDLE**、**gate.json 终态** | export_handoff 在 TestMind 侧已有；**Token-Mind 聚合 handoff 模板/校验未 blocking** |
| **stack-router** | RequirementMind READY → Bundle → Code → **TestMind VERIFY** → Brain；ConciseMind 仅 overlay | **全链 STALE 展示、VERIFY 节点 UI** 未达标（审计 §16） |
| **Context 压缩** | Output Gate / Read Guard / MCP Guard 三列账 | 治理层 77 测本地绿；**不得删减** [`test-Mind/skills/testmind/SKILL.md`](../test-Mind/skills/testmind/SKILL.md) 四条硬规则 — **无自动回归防删** |
| **Prompt Lab** | Mode D 未装=显式降级；eval 不伪造 | **未决**：评分标定、Layout auto-move、**G-S8-06** 失败待排期；大量 prompt-engine **git untracked** |
| **安装/Doctor** | `contextmind/cli.mjs install/doctor` 任意 consumer PASS | 与 TestMind peak-sim **联合 doctor** 未定义 |
| **独立克隆** | 不依赖工作区外绝对路径（INV-001） | 需与 monorepo `shared/` 策略一致 |

### 1.3 Token-Mind 专项缺陷清单

| ID | 严重度 | 问题 | 满分要求 |
|----|--------|------|----------|
| TM-CTX-01 | P0 | G0–G8 未全部执行 | 每门 blocking 脚本 + 证据路径入库 |
| TM-CTX-02 | P0 | TaskBundle → TestMind MCP **无产品级一键路由**（仅 schema + 手工 JSON） | SDLC `task.active` + MCP 配置模板化 |
| TM-CTX-03 | P1 | `spec.verify.change_manifest` 未强制 ai-code 提交后写入 | CI 拒绝无 manifest 的 peak task |
| TM-CTX-04 | P1 | handoff 字段未在 ContextMind 层 **schema 校验** | 缺 TEST_REPORT/gate 摘要 → BLOCKED |
| TM-CTX-05 | P1 | 全链 PASS/STALE **无统一 Peak 仪表盘** | INV-006 用户可见 |
| TM-CTX-06 | P2 | Prompt Lab round2–4 **未决项**（标定、S8-06、untracked） | 清零未决或显式 NOT_REQUIRED |
| TM-CTX-07 | P2 | 压缩/Skill 冲突无 **Contract test** | TestMind Skill 关键词保真门禁 |

---

## 2. TestMind（VERIFY 层）— 审计缺陷 + 整改状态

> 下列 **P0-1～P0-10** 来自审计 §5（GitHub 基线事实）；**工作区 test-Mind** 标注 **已修 / 部分 / 未修**（以附录 C 与代码为准）。

### 2.1 P0 源码级缺陷（跨 AI 可信）

| ID | 缺陷摘要 | 满分规格 | 工作区状态 |
|----|----------|----------|------------|
| **P0-1** | 规划器硬编码 `influencer_id` / `idem_key` | Contract 驱动 relation/idempotency 字段 | **已修**（planner + generic；红包仅 example hooks） |
| **P0-2** | OpenAPI 事实全局串线 | operation 作用域 + `facts_for_operation` | **已修**（`test_openapi_operation`） |
| **P0-3** | 无 task_id / change_manifest / base..head | TaskBundle intake + git range | **部分**：intake + `git_change`；**Manifest 与 drift 全链未满分** |
| **P0-4** | 全局 regression 串项目 | consumer 仓命名空间 + compat 检查 | **部分**：隔离单测有；**默认路径/Brain 索引未满分** |
| **P0-5** | MCP schema 全错 `{args:{}}` | 每工具 inputSchema + contract tests | **已修**（`test_mcp_schema`） |
| **P0-6** | 凭证明文进证据 | SecretGuard 五出口 | **已修**（`test_secrets`）；**URL query/日志扫描需持续扩展** |
| **P0-7** | production 无保险 | env_class + deny destructive | **已修**（`test_env_policy`）；**allowlist/capability token 钩子未满分** |
| **P0-8** | required 工具跳过仍 PASS | Risk Router + Gate | **部分**：`test_gate_bindings`；**Schemathesis/Karate 矩阵未生产 blocking** |
| **P0-9** | MCP 无四件套 | TEST_PLAN/CASES/MANIFEST/REPORT + FAILURE_BUNDLE | **已修**（export + peak-sim）；**模板非全 Java 域** |
| **P0-10** | 单例 Session 无恢复 | task_id 持久化 + state.json | **部分**：session_store + 单测；**崩溃恢复 E2E、多进程锁未满分** |

### 2.2 P1 能力与工程缺陷（仍未达 9 分）

| ID | 缺陷摘要 | 满分规格 | 状态 |
|----|----------|----------|------|
| **P1-01** | Docker 缺失曾崩溃 | 三态 UNAVAILABLE/SKIPPED | **已修** |
| **P1-02** | Schemathesis/Karate 路径写死 Windows | 可配置 + 三态 | **部分** |
| **P1-03** | POSIX SUT cleanup 进程组 | `_killpg` + 单测 | **已修** |
| **P1-04** | peak-gate 依赖 `../../shared` | vendor/submodule | **部分**（fallback lib）；**独立克隆仍风险** |
| **P1-05** | 无依赖锁 / `pip install -e` 镜像失败 | 可解析 pyproject + CI | **部分**（pyproject 有；**用户镜像缺 setuptools 未文档化**） |
| **P1-06** | Linux CI TM3 未实测 | Actions matrix 绿 | **NOT_RUN**（workflow 已加） |
| **P1-07** | Facts 冲突检测弱 | 数值区间/HTTP 码/状态机冲突 | **未修满** |
| **P1-08** | unparsed 曾丢弃 | MCP 返回 + Gate | **已修** |
| **P1-09** | HTTP：multipart/query/cookie/OAuth 弱 | Risk Router 覆盖真实 API | **未修满** |
| **P1-10** | 无 Redis/PG/MQ 断言 | adapter + 环境页 | **未做** |
| **P1-11** | 「压测」非 k6 | k6 + P95/P99 + 基线 | **未做** |
| **P1-12** | Java 理解 ≈2/10 | Spring/MyBatis 扫描 + plan | **起步**（`scan_java`）；**无 shejiuPro E2E** |
| **P1-13** | 文档/门禁数字漂移 | README/verify.ps1 与实测一致 | **未全仓清扫** |
| **P1-14** | MCP 仍 ~29 工具 | 收敛 10–12 Agent 面 | **未做** |
| **P1-15** | `analyze_change` / DRIFT_REPORT | Manifest × git × endpoints 交叉 | **未修满** |
| **P1-16** | 环境配置 UI | FlowMind/Agent OS + `environment_ref` | **无 UI**（peak-sim 仅 JSON 档案） |
| **P1-17** | JaCoCo/覆盖率 NOT_TESTED | Gate required | **未做** |
| **P1-18** | ResourceWarning / 文件未 close |  hygiene 不影响 PASS 但降工程分 | **未修** |
| **P1-19** | sim_harness  curated 前清 facts | 应用 risk case 或 sim 不注册 risk topic | **设计债** |
| **P1-20** | Windows TM3 代位 TM1 | Linux 114/114 blocking | **机制性缺口** |

### 2.3 P2 仓库与架构

| ID | 缺陷摘要 | 满分规格 | 状态 |
|----|----------|----------|------|
| **P2-01** | 仓内 RequirementMind / recovery JSON 污染 | W-P0-13 去污 | **未做** |
| **P2-02** | core.py 上帝对象 | 拆 facts/planner/executors/gate/adapters | **未做** |
| **P2-03** | 无 Dashboard / 多任务队列 | §14 P2 | **未做** |
| **P2-04** | 基准集 30–50 真实 task | 漏测率/误报率 | **未做** |
| **P2-05** | GitHub 镜像 vs 工作区漂移 | 整改 PR 同步 test-mind | **未确认** |

### 2.4 P0 工程工单 W-P0-01～13 对照

| 工单 | 摘要 | 状态 |
|------|------|------|
| W-P0-01 | MCP Schema | **完成** |
| W-P0-02 | Task 持久化 | **大部分** |
| W-P0-03 | Intake | **大部分** |
| W-P0-04 | Change 分析 | **部分** |
| W-P0-05 | Planner 去硬编码 | **完成** |
| W-P0-06 | Facts operation | **大部分** |
| W-P0-07 | Regression 隔离 | **部分** |
| W-P0-08 | SecretGuard | **完成** |
| W-P0-09 | Env Gate | **大部分** |
| W-P0-10 | 四件套导出 | **完成** |
| W-P0-11 | Gate required | **大部分** |
| W-P0-12 | CI + peak-gate TM0–TM9 + 超时 | **大部分**（Linux CI NOT_RUN） |
| W-P0-13 | 仓内去污 | **未完成** |

---

## 3. 栈对齐 — shared 不变量与 contract 缺口

| 不变量 | 含义 | 问题 |
|--------|------|------|
| **INV-001** | contract 无 Windows 绝对路径 | 各仓脚本仍有个体 `E:\` 探针（如历史 verify_restored） |
| **INV-002** | workspace 由 sibling repo 发现 | monorepo OK；**单仓 clone 策略未文档化** |
| **INV-003** | installed copy 非 canonical | 需防 AI 改 site-packages 副本 |
| **INV-004** | optional = NOT_INSTALLED | FlowMind 等待遇一致 |
| **INV-005** | gate 信封字段完整 | TestMind **部分**绑定；Token-Mind G 门未全 |
| **INV-006** | PASS → STALE | 单测有；**栈聚合 UI 无** |
| **INV-007** | Brain V3 单写 | 与 TestMind 回写边界需 INT 实测 |
| **INV-008** | Candidate 幂等 NO_OP | Brain 侧；与 VERIFY 无关但 Peak 依赖 |
| **INV-009** | TaskBundle v2 session_id | schema 有；**全链强制未满分** |
| **INV-010** | evidence relative_ref + content_hash | TestMind manifest 有；**跨仓聚合未满分** |

**contract 不一致**

- [`pipeline-contract.yaml`](../shared/pipeline-contract.yaml) TestMind gates 列为 **TM0–TM8**；实现已增 **TM9**（peak-sim）→ **contract 未 bump**（文档/contract 漂移）。
- **shejiu_integration INT0–INT2**：consumer 全链 **未在附录 C 填 PASS**。

---

## 4. §15 P0 验收门槛 — 逐项对照（满分前置）

审计 §15「只有全部满足才可从 HOLD 变为试点」— 对工作区 **诚实勾选**：

| # | 门槛 | 状态 |
|---|------|------|
| 1 | Windows + Linux CI 全绿 | **部分**（Win 绿；Linux NOT_RUN） |
| 2 | 适配器缺失三态 | **PASS** |
| 3 | 100% MCP schema + contract tests | **PASS**（工具数仍过多） |
| 4 | 两项目并行不串 | **PASS**（单测） |
| 5 | base/head SHA 影响面 | **部分** |
| 6 | 通用仓无红包字段 + ≥3 领域 | **部分**（generic 3 域；**非 3 个独立 consumer repo**） |
| 7 | 无 body/GET/幂等/租户/Redis 各 E2E | **部分**（generic/peak-sim；**缺 Redis/租户**） |
| 8 | 合成凭证 evidence=0 | **PASS** |
| 9 | production destructive 拒 | **PASS** |
| 10 | 四件套自动生成 | **PASS**（sim/export） |
| 11 | required unavailable 不得 PASS | **大部分** |
| 12 | 崩溃按 task_id 恢复 | **未满分** |
| 13 | 注错 ≥9/10 | **PASS** (10/10) |
| 14 | **真实 Java 全链** | **FAIL / 未做** |

→ §15 **未全满足**；§16 **PILOT** 合理，**非 9 分**。

---

## 5. 9 分里程碑（执行顺序）

| 里程碑 | 交付 | 估分 |
|--------|------|------|
| **M1** | shejiuPro（或等价）TaskBundle + TestMind intake + ≥20 P0 case 绿 | 7 |
| **M2** | Linux CI TM3 114/114 + W-P0-13 去污 + contract 写入 TM9 | 7.5 |
| **M3** | k6 smoke + JaCoCo 阈值 + Redis/MyBatis facts → Gate required | 8 |
| **M4** | Peak 全链 demo + STALE 聚合可见 + Token-Mind G0–G8 绿 | 8.5 |
| **M5** | 第二 consumer + 30 天运营指标 + MCP 收敛 12 工具 | **9** |

---

## 6. 已缓解但需写入回归的「卡顿/ flaky」问题

| 问题 | 根因 | 修复 | 回归要求 |
|------|------|------|----------|
| 全量单测 peak_sim FAIL | `import sut` 绑定红包模块 | `importlib` 加载 generic/sut | `test_peak_simulation` 必须在 discover 后跑 |
| peak-gate 无限挂起 | spawnSync 无 timeout | timeoutMs + `run_peak_verify.py` | CI job timeout 25min |
| Cursor 工具 278s 中断 | 大范围 Grep/子 agent | 缩小路径；避免全仓 grep | 流程规范，非代码 |

---

## 7. 复现与验收命令（当前 PILOT 证据）

```bash
# TestMind（工作区）
cd test-Mind
python scripts/run_peak_verify.py          # 推荐：unittest + TM0–TM9，带超时
python scripts/peak_simulation.py          # peak-sim 全链
python -m unittest discover -s tests -q    # 79 tests

# Token-Mind
cd Token-Mind
node --test "contextmind/tests/*.test.mjs" # 77 用例（与 TestMind 独立）
```

**证据路径**

- Peak 模拟：`test-Mind/examples/peak-sim/.testmind/last-simulation.json`
- 审计实测：[`TestMind_…报告_20260910.md`](../TestMind_真实源码全量审计与跨AI极致测试整改报告_20260910.md) 附录 C

---

## 8. 文档维护规则

1. 任一 **PASS/9 分** 宣称前，必须更新本节 **状态列** 并附 **证据路径 + 命令 + 日期**。  
2. 审计 Part A（§1–§7）**事实不得删改**；新结论写入 **状态列** 或新附录。  
3. Token-Mind 与 TestMind 问题 **分 ID 前缀**（`TM-CTX-*` / `P0-*`），避免混仓。  
4. `pipeline-contract.yaml` 变更时同步 **INV-006 STALE** 与本文 §3。

---

**一句话**：满分 = **F1–F10**；当前 **PILOT ≈6.5/10**；**最大缺口** = 真实 Java consumer 全链、Linux CI、G0–G8、栈级 STALE/UI、k6/覆盖/Redis、仓去污与 MCP 收敛。
